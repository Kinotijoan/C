#include <stdio.h>

int main(void)
{
    printf("test\n");
    // char *my_string;
    // asprintf(&my_string, "I like %d number , but my lucky number is 10.",4);
    // puts(my_string);
    // asprintf(&my_string, "i like %s number , but i have cake.", "cake");
    // puts(my_string);
    return (0);
}